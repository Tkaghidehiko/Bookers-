<div class="container">
	<h1>welcom to Bookers !!</h1>
	<p>In Bookers,<br>you can share and exchange your opinions , inpressions , and emotions<br>about varioius books!
	</p>
	<% if user_signed_in? %>
	<%= link_to "Log In", user_path(current_user.id), class: "book_#{current_user.id}" %>
	<% else %>
	<%= link_to "Log In", new_user_session_path, class: "sign_in" %>
	<% end %>
	<br>
	<%= link_to "Sign Up", new_user_registration_path, class: "sign_up" %>